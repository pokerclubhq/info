---
title: Player Directory
description: How to manage your club's roster — contact details, CSV import/export, merging duplicates, and more — in Poker Club HQ.
---

import { Image } from 'astro:assets';
import playerDir00001 from '../../assets/player-dir/player-dir-00001.png';
import playerDir00002 from '../../assets/player-dir/player-dir-00002.png';
import playerDir00003 from '../../assets/player-dir/player-dir-00003.png';
import playerDir00004 from '../../assets/player-dir/player-dir-00004.png';
import playerDir00005 from '../../assets/player-dir/player-dir-00005.png';
import playerDir00006 from '../../assets/player-dir/player-dir-00006.png';
import playerDir00007 from '../../assets/player-dir/player-dir-00007.png';
import playerDir00008 from '../../assets/player-dir/player-dir-00008.png';
import playerDir00009 from '../../assets/player-dir/player-dir-00009.png';
import playerPhotos00006 from '../../assets/player-photos/player-photos-00006.png';

The Player Directory is one place to manage every player across all your tournaments. While [Managing players](/info/players/) covers the players in a single tournament, the directory is your club's full roster: see which tournaments each player has been in, keep contact info and notes, import and export player lists, and clean up duplicates.

## Opening the Player Directory

Open the main menu and tap **Player Directory**.

<Image src={playerDir00001} alt="The main menu with the Player Directory entry highlighted" width={320} />

## The player list

The header shows a summary of your roster — how many players you have, and how many aren't in any tournament. Each player tile shows the player's [photo](#player-photos) (or their initials when they don't have one) and the number of tournaments they appear in.

<Image src={playerDir00002} alt="The Player Directory list showing a player count summary and each player's photo and tournament count" width={320} />

Use the buttons in the top bar to:

- **Search** — Filter the list as you type. Search matches names, nicknames, and email addresses.
- **Filter by group** — Appears once you've created a group; show only the players in one group. See [Groups](#groups).
- **Add a player** — Add someone to your roster before they've played in any tournament.
- **More (⋯)** — Sorting, CSV import/export, and cleanup.

## Groups

Groups are tags for organizing your roster — "Regulars", "Tuesday league", whatever fits your club. A player can belong to any number of groups.

There's no separate screen for managing groups: a group exists as long as at least one player belongs to it, and any **New group…** button can create one.

### Adding a player to groups

On a player's details screen, tap **+** and choose **Add to group…** — or, if the player is already in groups, tap the row listing them. A sheet shows every group with a checkbox; check the ones the player belongs to, or tap **New group…** to create one.

<Image src={playerDir00008} alt="The groups sheet on a player's details screen with a checkbox per group and New group, Cancel, and Save buttons" width={320} />

### Adding many players at once

In the directory's ⋯ menu, choose **Add players to a group…**. Select the players (there's a select-all checkbox in the top bar), tap **Next**, and pick the group — or create a new one.

### Filtering by group

Once a group exists, a filter button appears in the directory's top bar. Tap it to show only the players in one group; choose **All players** to clear the filter.

<Image src={playerDir00009} alt="The group filter menu in the Player Directory with All players checked and a Regulars group below" width={320} />

:::tip
Groups make tournament setup fast: on a tournament's Players screen you can add a whole group's players at once — see [Managing players](/info/players/#adding-players).
:::

## Sorting, import & export

The ⋯ menu sorts the list by **name**, by **recently changed**, or by **most tournaments** — and holds the roster-wide tools.

<Image src={playerDir00003} alt="The Player Directory menu with sort options, CSV import and export, and bulk delete" width={320} />

### Export CSV

Exports your whole roster as a CSV file with columns for Name, Nickname, Email, Phone, Street Address, City, State, Zip, Country, Notes, and Groups (semicolon-separated) — ready for spreadsheets or a backup.

### Import from CSV file

Brings players in from a CSV file. The importer accepts the app's own exports as well as player lists exported from Tournament Director (First/Last Name columns, "Email 1", "Phone 1", "Zipcode", and similar variations are recognized). A **Groups** (or **Group**) column with semicolon-separated group names is recognized too — memberships from the file are added to each player, never removed.

Importing is safe to repeat: new players are added with all their fields, while players already in your directory only have their *empty* fields filled in — existing information is never overwritten.

### Delete players not in any tournament

Bulk cleanup for roster entries that never played. Players who appear in a tournament can't be deleted, so this only offers the unused ones.

## Player details

Tap a player to open their details. The pencil renames the player, and the list below shows every tournament they appear in — tap one to open it.

<Image src={playerDir00004} alt="A player's details screen showing their name and the tournaments they appear in" width={320} />

:::note
Renaming a player here updates them everywhere — in every tournament they appear in.
:::

The sort button orders the tournament list by date or by name, and long histories start collapsed with a "Show all" row.

<Image src={playerDir00006} alt="The tournament list sort menu with newest first, oldest first, and name ordering" width={320} />

## Player photos

Give a player a photo by tapping the circle at the top of their details screen (or the **Add photo…** button below it). Pick an image and it's cropped to a circle. Once a player has a photo, the button reads **Edit photo…** and lets you replace or remove it.

<Image src={playerPhotos00006} alt="The photo dialog with Remove photo and Replace photo choices" width={320} />

The photo appears everywhere the player does: in the directory list, on a tournament's [Players screen](/info/players/), and at their seat on the [Tables screen](/info/tables/) — which can even show every seated player as a large photo; see [Player photos or names](/info/tables/#player-photos-or-names). Players without a photo show their initials in the player's color instead.

:::note
Photos require being signed in — they're stored with your account and shown on all your devices. When you [merge a duplicate](#merging-duplicates), the kept player's photo wins; if they don't have one, the duplicate's photo is used.
:::

## Contact info & notes

Tap **+** in the top bar to add notes, a nickname, an email address, a phone number, or mailing address fields to the player. Anything you add appears on the details screen, where you can tap it to edit.

<Image src={playerDir00005} alt="The add menu on a player's details screen listing notes, nickname, email, phone, and address fields" width={320} />

## Merging duplicates

The same person can end up in your directory twice — say, "Bob" in one tournament and "Bob Smith" in another. On the details screen of the player you want to *keep*, tap **Merge a duplicate into this player…** and pick the duplicate. The duplicate's tournament history moves to the kept player, their notes are combined, and any contact fields the kept player was missing are filled in from the duplicate.

<Image src={playerDir00007} alt="The bottom of a player's details screen with move tournaments, merge, and delete actions" width={320} />

:::note
Two players who appear in the *same* tournament can't be merged — they must be different people, since a tournament never lists the same player twice.
:::

:::tip
Merging also combines the players' rows in [Club stats](/info/club-stats/), so a season leaderboard no longer splits one person's results across two names.
:::

## Moving tournaments to another player

The opposite of a merge: one directory entry actually covers two different people with the same name. Tap **Move tournaments to another player…**, check the tournaments that belong to the other person, and choose an existing player — or type a new name to create one. The selected tournaments are reassigned, splitting the history between the two players.

## Deleting players

**Delete** on the details screen permanently removes a player, but only when they don't appear in any tournament — players with history can't be deleted (merge them instead). To clear out many unused entries at once, use **Delete players not in any tournament…** in the directory's ⋯ menu.
